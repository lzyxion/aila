"""전역 설정(`app_settings`) 조회·수정 API.

설계 문서가 전역 일일 분석 한도·모델 단가표·샘플 보존 기간을 **코드가 아니라 키-값
테이블**에 둔 이유는 하나다 — 배포 없이 바꿀 수 있어야 하기 때문이다. 읽는 코드만
있고 쓰는 경로가 없으면 그 결정이 절반만 구현된 셈이라, 결국 사람이 DB 에 직접 UPDATE
를 치게 된다 (그러면 값 형식 검증이 아무 데도 없다).
"""

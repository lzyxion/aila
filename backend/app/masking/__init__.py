"""민감정보 마스킹.

Phase 1 담당 트랙: **그룹화·마스킹** (`app/grouping` + `app/masking`)

최소 규칙(설계 문서 "오류 그룹화와 마스킹"):
- API 키, Bearer/JWT, 비밀번호, 쿠키, DB 연결 문자열
- 이메일, 전화번호, 카드번호 등 개인 식별 가능 정보
- 사용자 정의 정규식 기반 조직별 규칙

계약상 제약:
- 마스킹은 **화면 표시 전과 LLM 전송 직전에 모두** 적용한다. 두 경로가 서로 다른
  코드를 타므로 한쪽만 걸면 다른 쪽이 조용히 샌다.
- 처리 순서는 **마스킹 → 정규화 → fingerprint** 로 고정한다.
- 마스킹 전 원문은 **DB 에 저장하지 않는다.** `error_samples.masked_log` 에는 반드시
  마스킹을 거친 값만 넣는다.
- 마스킹 규칙 버전을 `error_samples.masking_rule_version` 에 저장한다. 규칙 강화는
  이미 저장된 샘플에 소급되지 않는다.
- 이 모듈에서 되돌릴 수 없는 유일한 실패가 나온다. 우선순위 다툼이 생기면 마스킹이 먼저다.

필수 자동 테스트: **LLM 요청 페이로드에 원문 토큰이 없음을 단언하는 테스트.**
"""

#: 마스킹 규칙 버전. 규칙을 고치면 반드시 올린다.
MASKING_RULE_VERSION = "v1"
